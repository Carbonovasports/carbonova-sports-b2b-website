
const toggle=document.querySelector('.menu-toggle');const mobile=document.querySelector('.mobile-menu');if(toggle&&mobile){toggle.addEventListener('click',()=>{const open=mobile.classList.toggle('open');toggle.setAttribute('aria-expanded',String(open));});}
document.querySelectorAll('[data-year]').forEach(el=>el.textContent=new Date().getFullYear());
const forms=document.querySelectorAll('[data-rfq-form]');forms.forEach(form=>{form.addEventListener('submit',e=>{e.preventDefault();const data=new FormData(form);const subject=encodeURIComponent('RFQ from '+(data.get('company')||data.get('name')||'website visitor'));const body=encodeURIComponent(['Name: '+(data.get('name')||''),'Company: '+(data.get('company')||''),'Country: '+(data.get('country')||''),'Email: '+(data.get('email')||''),'Product: '+(data.get('product')||''),'Quantity: '+(data.get('quantity')||''),'Message: '+(data.get('message')||'')].join('\n'));const alert=form.querySelector('.alert');if(alert){alert.classList.add('show');alert.textContent='Your email application will open with the RFQ details. Please verify the recipient before sending.';}window.location.href='mailto:sales@carbonovasports.com?subject='+subject+'&body='+body;});});
